The text used in one of the experiments with LexNET on cross-domain evaluation.

The text was parsed with the parse_wikipedia script.

entities.txt is a list of entities as recognized by the script (using SpaCy).
It can be added to a vocabulary to make sure LexNET recognizes all entities.
